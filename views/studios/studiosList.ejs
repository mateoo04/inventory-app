<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Studios</title>
    <script>
      function confirmDeletion() {
        return confirm('Are you sure you want to delete this move?');
      }
    </script>
    <link rel="stylesheet" href="/styles/styles.css" />
  </head>
  <body>
    <%- include('../partials/header') %>
    <button class="add-button" onclick="window.location.href='/studios/new'">
      +
    </button>
    <main>
      <h2>Studios</h2>
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Location</th>
          </tr>
        </thead>
        <tbody>
          <% if(studios){ %> <% studios.forEach(studio => { %>
          <tr>
            <td><%= studio.name %></td>
            <td><%= studio.location %></td>
            <td class="iconsContainer">
              <a href="/studios/<%= studio.id %>/edit">
                <img src="/icons/edit-icon.svg" alt="Edit"
              /></a>
              <form
                action="/studios/<%= studio.id %>/delete?_method=DELETE"
                method="post"
                onsubmit="return confirmDeletion()"
              >
                <input type="hidden" name="_method" value="DELETE" />
                <button type="submit">
                  <img src="/icons/trash-icon.svg" alt="Delete" />
                </button>
              </form>
            </td>
          </tr>
          <%});}%>
        </tbody>
      </table>
    </main>
  </body>
</html>
